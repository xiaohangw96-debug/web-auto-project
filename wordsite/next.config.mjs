/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "export",
  basePath: "/web-auto-project",
  images: { unoptimized: true },
};

export default nextConfig;
